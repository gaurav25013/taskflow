# Backend (set these in Railway)
PORT=5000
JWT_SECRET=your-super-secret-key-change-this
DB_PATH=./taskflow.db
NODE_ENV=production

# Frontend (set in Vite if separate deployment)
# VITE_API_URL=https://your-railway-app.up.railway.app
